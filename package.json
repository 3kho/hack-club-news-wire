{
  "name": "hack-club-news-wire",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "@slack/bolt": "^4.7.3",
    "@slack/types": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.63.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
